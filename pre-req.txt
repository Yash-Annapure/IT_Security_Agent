One-time per machine (makes scans near-instant afterwards):
uv run warm_cache.py --days=45     ~35-45 min, caches ~95% of NVD's CVEs
                                   (plain `uv run warm_cache.py` is a ~2 min
                                    quick warm, but much thinner coverage)

Every boot:
uv run serve.py (For mcp)
uv run serve_mistral.py (For model)

Cloudlfare tunnel

./cloudflared tunnel --url http://localhost:8000
./cloudflared tunnel --url http://localhost:8765

model:id Qwen/Qwen2.5-14B-Instruct
